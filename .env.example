# ============================================
# Configurazione Sistema Biblioteca
# R∴ L∴ Kilwinning
# ============================================
#
# ISTRUZIONI:
# 1. Copia questo file in .env: cp .env.example .env
# 2. Modifica i valori con le credenziali corrette
# 3. NON committare .env in Git (contiene credenziali sensibili)
#

# ============================================
# Database Configuration
# ============================================
# Host del database (di solito 'localhost' o IP del server DB)
DB_HOST=localhost

# Porta del database MySQL (default: 3306)
DB_PORT=3306

# Username per accesso al database
DB_USERNAME=biblioteca_user

# Password per accesso al database
DB_PASSWORD=password_here

# Nome del database
DB_DATABASE=biblioteca_db

# Charset del database (non modificare se non necessario)
DB_CHARSET=utf8mb4

# ============================================
# Security Settings
# ============================================
# Timeout sessione in secondi (default: 1800 = 30 minuti)
SESSION_TIMEOUT=1800

# Admin IDs - (DEPRECATED: preferire campo 'role' nel database)
# Mantenuto per backward compatibility
ADMIN_IDS=1,2,3,4

# ============================================
# CORS Configuration (per API)
# ============================================
# Domini autorizzati per CORS (comma separated)
# Usare * solo in development, mai in production!
# Esempio: https://biblioteca.loggiakilwinning.com
CORS_ALLOWED_ORIGINS=
